#############################################
# 🌑 PSYCHO-NOIR KONTRAPUNKT – ENV TEMPLATE #
#############################################

# Tavily Web Intelligence Arterie Key
# Obtain from: https://app.tavily.com/
# Export into your shell or copy to a local .env (DO NOT COMMIT REAL KEY)
TAVILY_API_KEY=REPLACE_WITH_YOUR_TAVILY_KEY

# Optional: Hugging Face token (for gated/private repos)
HF_TOKEN=REPLACE_WITH_HF_TOKEN

# Future: Google Custom Search (Programmable Search Engine)
GOOGLE_API_KEY=REPLACE_OR_EMPTY
GOOGLE_SEARCH_ENGINE_ID=REPLACE_OR_EMPTY

# Gemma model path customization (override default mock path)
GEMMA_MODEL_PATH=models/gemma-300m-q4.gguf

# Low memory mode toggle (true/false)
GEMMA_LOW_MEM=true

#############################################
# Usage:
#   cp .env.example .env
#   (edit values)
#   export $(grep -v '^#' .env | xargs)
#
# In Bun scripts you can load via: Bun.env.TAVILY_API_KEY
#############################################
